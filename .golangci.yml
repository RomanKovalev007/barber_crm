version: "2"

linters:
  enable:
    - govet
    - ineffassign
    - staticcheck
    - misspell
    - unconvert
    - unparam
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam
